<%= form_with(model: battle, local: true) do |form| %>
  <% if battle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(battle.errors.count, "error") %> prohibited this battle from being saved:</h2>

      <ul>
      <% battle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :firstpersonnage %>
    <%= form.number_field :firstpersonnage, id: :battle_firstpersonnage %>
  </div>

  <div class="field">
    <%= form.label :secondpersonnage %>
    <%= form.number_field :secondpersonnage, id: :battle_secondpersonnage %>
  </div>

  <div class="field">
    <%= form.label :winner %>
    <%= form.number_field :winner, id: :battle_winner %>
  </div>

  <div class="field">
    <%= form.label :recapfight %>
    <%= form.text_area :recapfight, id: :battle_recapfight %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
