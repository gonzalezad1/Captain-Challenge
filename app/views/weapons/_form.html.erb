<%= form_with(model: weapon, local: true) do |form| %>
  <% if weapon.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weapon.errors.count, "error") %> prohibited this weapon from being saved:</h2>

      <ul>
      <% weapon.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :weapon_name %>
  </div>

  <div class="field">
    <%= form.label :attack %>
    <%= form.number_field :attack, id: :weapon_attack %>
  </div>

  <div class="field">
    <%= form.label :defense %>
    <%= form.number_field :defense, id: :weapon_defense %>
  </div>

  <div class="field">
    <%= form.label :velocity %>
    <%= form.number_field :velocity, id: :weapon_velocity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
